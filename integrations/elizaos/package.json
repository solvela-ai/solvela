{
  "name": "@solvela/elizaos-plugin",
  "version": "0.1.0",
  "description": "ElizaOS plugin for Solvela — Solana-native AI agent payment gateway",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "echo 'no tests yet'"
  },
  "peerDependencies": {
    "@elizaos/core": ">=0.2.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
