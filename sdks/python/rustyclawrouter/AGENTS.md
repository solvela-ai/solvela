<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-16 | Updated: 2026-04-16 -->

# rustyclawrouter

## Purpose
Legacy compatibility shim for the `Solvela → Solvela` rebrand. Exists so users who `import rustyclawrouter` keep working without code changes. All real logic lives in `../solvela/`; this package only re-exports.

## Key Files
| File | Description |
|------|-------------|
| `x402.py` | Re-exports the x402 helpers from `solvela.x402` (legacy import path) |
| `__pycache__/` | Generated bytecode — not checked in |

## Subdirectories
_(none)_

## For AI Agents

### Working In This Directory
- **Do not add new features here.** All new code goes in `../solvela/`.
- At most, extend this module to re-export additional names already defined in `solvela` under their old Solvela-era aliases.
- When the deprecation window closes, this package will be removed entirely.

### Testing Requirements
```bash
pytest sdks/python
```
Tests should verify that the old import paths still resolve to the canonical implementations.

### Common Patterns
- Flat `from solvela.foo import Bar as LegacyBar` style re-exports.

## Dependencies

### Internal
- `../solvela/` — the real implementation.

### External
_(none direct)_

<!-- MANUAL: -->
