app = "solvela-gateway"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile"

# Release command runs once before any new machine starts serving traffic.
# Migrations live here so a broken schema fails the deploy instead of crashing
# every running instance mid-flight (see CLAUDE.md rule #15).
[deploy]
  release_command = "/app/solvela-gateway migrate"

[env]
  PORT = "8402"
  RUST_LOG = "gateway=info,tower_http=info"

[http_service]
  internal_port = 8402
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"
    protocol = "http"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
